# ChalkLens — sample environment file. Copy to .env and fill in.
# .env is gitignored; .env.example is committed as a template.

# Public URL of the LiteRT-LM model file the app downloads on first launch.
# Required by scripts/run.sh and scripts/build-release-apk.sh.
GEMMA_MODEL_URL=
